# Python virtual environment — never commit (huge, machine-specific)
venv/
.venv/

# Dataset — too large for git; download from Harvard Dataverse (see README)
data/

# Trained model weights — regenerate by running the scripts
models/

# Python cache
__pycache__/
*.pyc
*.pyo

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
